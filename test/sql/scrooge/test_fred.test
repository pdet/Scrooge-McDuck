# name: test/sql/scrooge/test_fred.test
# description: Test FRED scanner function registration
# group: [scrooge]

require scrooge

# Verify the function exists (will fail at HTTP level without valid API key, but that's expected)
statement error
SELECT * FROM fred_series('GDP', 'invalid_key');
----
